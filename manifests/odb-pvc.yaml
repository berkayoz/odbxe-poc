apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: odbxe-poc-pvc
  namespace: odbpoc
spec:
  storageClassName: default
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi